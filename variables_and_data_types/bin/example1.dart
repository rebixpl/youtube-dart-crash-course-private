void main(List<String> args) {
  const name = "foo";
  print(name);
  // invalid code
  // name = "hello";

  // Invalid code
  // const age = [1, 2, 3];
  // age.removeAt(0);
  // print(age); // Unsupported operation: Cannot remove from an unmodifiable list
}
